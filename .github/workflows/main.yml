name: CI/CD Pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # Check out the code
      - uses: actions/checkout@v3

      # Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # Lint the code
      - name: Lint
        run: npm run lint

      # Run tests with coverage
      - name: Run tests with coverage
        run: npm test -- --coverage
        env:
          CI: true

      # Debug coverage files
      - name: Debug Coverage Files
        run: |
          echo "Checking for coverage directory and files..."
          ls -la ./coverage || echo "Coverage directory not found"
          ls -la ./coverage/coverage-summary.json || echo "Coverage summary file not found"

  check-coverage:
    runs-on: ubuntu-latest
    needs: test
    steps:
      # Check out the code
      - uses: actions/checkout@v3

      # Ensure coverage report exists
      - name: Ensure Coverage File Exists
        run: |
          if [ ! -f ./coverage/coverage-summary.json ]; then
            echo "Coverage report not found! Creating a placeholder."
            mkdir -p ./coverage
            echo '{"total":{"lines":{"pct":0},"statements":{"pct":0},"functions":{"pct":0},"branches":{"pct":0}}}' > ./coverage/coverage-summary.json
          fi
          echo "Coverage report found or placeholder created."
          cat ./coverage/coverage-summary.json

      # Check test coverage
      - name: Check test coverage
        run: |
          COVERAGE=$(cat ./coverage/coverage-summary.json | jq .total.lines.pct)
          echo "Test coverage is ${COVERAGE}%"
          if (( $(echo "$COVERAGE < 90" | bc -l) )); then
            echo "Test coverage is below 90%, cannot deploy."
            exit 1
          fi

  build-and-deploy:
    needs: check-coverage
    runs-on: ubuntu-latest
    steps:
      # Check out the code
      - uses: actions/checkout@v3

      # Setup Node.js environment
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Install dependencies
      - name: Install dependencies
        run: npm ci

      # Build the project
      - name: Build dist
        run: npm run build

      # Setup GitHub Pages configuration
      - name: Setup Pages
        uses: actions/configure-pages@v3

      # Upload build artifacts to GitHub Pages
      - name: Upload dist folder
        uses: actions/upload-pages-artifact@v1
        with:
          path: './dist'

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
